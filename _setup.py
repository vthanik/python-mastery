"""Shared setup for code cells across chapters.

Most chapters don't need anything from this file. It exists so individual
chapters can `from _setup import *` if a future chapter needs shared
imports or helpers (matching the role of common.R in ggplot2-book).
"""

from __future__ import annotations
